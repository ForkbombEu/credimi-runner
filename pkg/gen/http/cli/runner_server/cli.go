// Code generated by goa v3.24.3, DO NOT EDIT.
//
// runner-server HTTP client CLI support package
//
// Command:
// $ goa gen github.com/forkbombeu/credimi-runner/pkg/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	runnerc "github.com/forkbombeu/credimi-runner/pkg/gen/http/runner/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"runner (process-start-missing|process-start|process-list|fetch-apk-and-action|store-pipeline-result|touch-fingerprint)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "runner process-start-missing" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		runnerFlags = flag.NewFlagSet("runner", flag.ContinueOnError)

		runnerProcessStartMissingFlags = flag.NewFlagSet("process-start-missing", flag.ExitOnError)

		runnerProcessStartFlags         = flag.NewFlagSet("process-start", flag.ExitOnError)
		runnerProcessStartBodyFlag      = runnerProcessStartFlags.String("body", "REQUIRED", "")
		runnerProcessStartNamespaceFlag = runnerProcessStartFlags.String("namespace", "REQUIRED", "")

		runnerProcessListFlags = flag.NewFlagSet("process-list", flag.ExitOnError)

		runnerFetchApkAndActionFlags    = flag.NewFlagSet("fetch-apk-and-action", flag.ExitOnError)
		runnerFetchApkAndActionBodyFlag = runnerFetchApkAndActionFlags.String("body", "REQUIRED", "")

		runnerStorePipelineResultFlags    = flag.NewFlagSet("store-pipeline-result", flag.ExitOnError)
		runnerStorePipelineResultBodyFlag = runnerStorePipelineResultFlags.String("body", "REQUIRED", "")

		runnerTouchFingerprintFlags = flag.NewFlagSet("touch-fingerprint", flag.ExitOnError)
	)
	runnerFlags.Usage = runnerUsage
	runnerProcessStartMissingFlags.Usage = runnerProcessStartMissingUsage
	runnerProcessStartFlags.Usage = runnerProcessStartUsage
	runnerProcessListFlags.Usage = runnerProcessListUsage
	runnerFetchApkAndActionFlags.Usage = runnerFetchApkAndActionUsage
	runnerStorePipelineResultFlags.Usage = runnerStorePipelineResultUsage
	runnerTouchFingerprintFlags.Usage = runnerTouchFingerprintUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "runner":
			svcf = runnerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "runner":
			switch epn {
			case "process-start-missing":
				epf = runnerProcessStartMissingFlags

			case "process-start":
				epf = runnerProcessStartFlags

			case "process-list":
				epf = runnerProcessListFlags

			case "fetch-apk-and-action":
				epf = runnerFetchApkAndActionFlags

			case "store-pipeline-result":
				epf = runnerStorePipelineResultFlags

			case "touch-fingerprint":
				epf = runnerTouchFingerprintFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "runner":
			c := runnerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "process-start-missing":
				endpoint = c.ProcessStartMissing()
			case "process-start":
				endpoint = c.ProcessStart()
				data, err = runnerc.BuildProcessStartPayload(*runnerProcessStartBodyFlag, *runnerProcessStartNamespaceFlag)
			case "process-list":
				endpoint = c.ProcessList()
			case "fetch-apk-and-action":
				endpoint = c.FetchApkAndAction()
				data, err = runnerc.BuildFetchApkAndActionPayload(*runnerFetchApkAndActionBodyFlag)
			case "store-pipeline-result":
				endpoint = c.StorePipelineResult()
				data, err = runnerc.BuildStorePipelineResultPayload(*runnerStorePipelineResultBodyFlag)
			case "touch-fingerprint":
				endpoint = c.TouchFingerprint()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// runnerUsage displays the usage of the runner command and its subcommands.
func runnerUsage() {
	fmt.Fprintln(os.Stderr, `Runner HTTP service.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] runner COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    process-start-missing: ProcessStartMissing implements process_start_missing.`)
	fmt.Fprintln(os.Stderr, `    process-start: ProcessStart implements process_start.`)
	fmt.Fprintln(os.Stderr, `    process-list: ProcessList implements process_list.`)
	fmt.Fprintln(os.Stderr, `    fetch-apk-and-action: FetchApkAndAction implements fetch_apk_and_action.`)
	fmt.Fprintln(os.Stderr, `    store-pipeline-result: StorePipelineResult implements store_pipeline_result.`)
	fmt.Fprintln(os.Stderr, `    touch-fingerprint: TouchFingerprint implements touch_fingerprint.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s runner COMMAND --help\n", os.Args[0])
}
func runnerProcessStartMissingUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner process-start-missing", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ProcessStartMissing implements process_start_missing.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner process-start-missing")
}

func runnerProcessStartUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner process-start", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ProcessStart implements process_start.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner process-start --body '{\n      \"old_namespace\": \"Vel esse.\"\n   }' --namespace \"Nesciunt aut architecto.\"")
}

func runnerProcessListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner process-list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ProcessList implements process_list.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner process-list")
}

func runnerFetchApkAndActionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner fetch-apk-and-action", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `FetchApkAndAction implements fetch_apk_and_action.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner fetch-apk-and-action --body '{\n      \"action_identifier\": \"Ab non et temporibus expedita ipsam omnis.\",\n      \"instance_url\": \"Voluptate placeat iure neque illo voluptatum quia.\",\n      \"version_identifier\": \"Quis debitis.\"\n   }'")
}

func runnerStorePipelineResultUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner store-pipeline-result", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `StorePipelineResult implements store_pipeline_result.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner store-pipeline-result --body '{\n      \"instance_url\": \"Similique eum perferendis amet corrupti iusto.\",\n      \"last_frame_path\": \"Maxime autem rerum accusamus quidem.\",\n      \"logcat_path\": \"Pariatur inventore quo itaque.\",\n      \"run_identifier\": \"Nobis asperiores.\",\n      \"runner_identifier\": \"Quos et ipsum autem soluta cumque sit.\",\n      \"video_path\": \"Rem error.\"\n   }'")
}

func runnerTouchFingerprintUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] runner touch-fingerprint", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `TouchFingerprint implements touch_fingerprint.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "runner touch-fingerprint")
}
